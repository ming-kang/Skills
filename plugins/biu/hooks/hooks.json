{
  "description": "Biu plugin hooks: subagent artifact verification + task-complete reminder (pure bash, zero runtime deps)",
  "hooks": {
    "PostToolUse": [
      {
        "comment": "Broad Edit|Write matcher is necessary because Claude Code hooks do not support path-based filtering. The notify-task-complete.sh script performs its own early-exit filtering to detect .biu/tasks/task-*.md files marked COMPLETE.",
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/hooks/notify-task-complete.sh"
          }
        ]
      }
    ],
    "SubagentStop": [
      {
        "matcher": "^biu:analyzer$",
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/hooks/verify-analyzer.sh"
          }
        ]
      },
      {
        "matcher": "^biu:architect$",
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/hooks/verify-architect.sh"
          }
        ]
      }
    ]
  }
}
