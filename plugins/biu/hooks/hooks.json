{
  "description": "Biu plugin hooks: subagent artifact verification (pure bash, zero runtime deps)",
  "hooks": {
    "SubagentStop": [
      {
        "matcher": "^analyzer$",
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/hooks/verify-analyzer.sh"
          }
        ]
      },
      {
        "matcher": "^architect$",
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/hooks/verify-architect.sh"
          }
        ]
      }
    ]
  }
}
