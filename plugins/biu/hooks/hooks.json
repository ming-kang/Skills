{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "if": "Edit(*.md)",
            "command": "${CLAUDE_PLUGIN_ROOT}/hooks/notify-task-complete.sh"
          },
          {
            "type": "command",
            "if": "Write(*.md)",
            "command": "${CLAUDE_PLUGIN_ROOT}/hooks/notify-task-complete.sh"
          }
        ]
      }
    ]
  }
}
